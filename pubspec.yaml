# congress_fahrplan
# This is the pubspec file specifying all dependencies
# SPDX-License-Identifier: GPL-2.0-only
# Copyright (C) 2019 - 2021 Benjamin Schilling

name: congress_fahrplan
description: Congress Fahrplan written in flutter

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.3.2

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.2
  path_provider: ^2.1.1
  provider: ^6.1.1
  flutter_linkify: ^6.0.0
  page_view_indicators: ^2.0.0
  url_launcher: ^6.2.1
  connectivity_plus: ^5.0.2
  flutter_svg: ^2.0.9
  intl: ^0.18.1
  share_plus: ^7.2.1
  device_calendar: ^4.3.2
  package_info_plus: ^5.0.1
  timezone: ^0.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  fonts:
    - family: MonaSans
      fonts:
        - asset: assets/MonaSans-Regular.ttf
        - asset: assets/MonaSans-Bold.ttf
          weight: 700
    - family: VcrOcdFaux
      fonts:
        - asset: assets/VCROCDFaux.ttf
    - family: GabriellaHeavy
      fonts:
        - asset: assets/GabriellaHeavy.otf

  assets:
    - assets/
